project_id = "cerebron-local"

[functions.cerebron-worker]
verify_jwt = false

# Authentication is enforced inside the function with CEREBRON_WORKER_TOKEN.
# No secrets are committed. Production deployment requires explicit secrets.
